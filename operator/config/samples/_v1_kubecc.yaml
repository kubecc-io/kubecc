apiVersion: kubecc.io/v1alpha1
kind: Kubecc
metadata:
  name: kubecc-sample
spec:
  nodes:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - matchExpressions:
            - key: allow-kubecc
              operator: In
              values: 
                - "true"
    resources:
      requests:
        memory: 500Mi
        cpu: 2
      limits:
        memory: 1Gi
        cpu: 4
  agentImage: kubecc/agent:testing
  mgrImage: kubecc/mgr:testing
  hostname: kubecc.example.com

        
